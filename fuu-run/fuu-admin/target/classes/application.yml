# 项目相关配置
fuu:
  # 名称
  name: Fuu-Run
  # 版本
  version: 1.0.1
  # 版权年份
  copyrightYear: 2030
  # 超时未支付取消时长 (分钟)
  pay-cancel-ttl: 15
  # 超时未完成自动完成时长 (小时)
  auto-complete-ttl: 24
  # 完成订单凭证上限 （张）
  completion-images-limit: 5
  # 信用分上限（初始）
  credit-upper_limit: 12
  # 信用分下限
  credit-lower-limit: 2
  # 信用分每次扣除
  credit-deduction: 1
  # 用户地址上限
  max-address: 20
  local-upload-enabled: true
  local-upload-dir: local-uploads
  local-upload-url-prefix: http://localhost:8081/local-uploads
email:
  host: localhost
  port: 25
  user: test@example.com
  pass: test

# 高德配置
amap:
  key: 999e66baca746f7ba3def14da9329c5a

rider:
  location:
    # 超过该时间未上报位置，骑手不参与附近骑手数量和附近订单召回
    expire-minutes: 5
    # 用户选择取货地址后，默认统计附近骑手半径
    nearby-count-radius-km: 3
    # 骑手订单大厅默认召回订单半径
    nearby-order-radius-km: 5

dispatch:
  # 第一轮：优先推给取货点 2km 内 Top5 骑手，30 秒无人接单后扩圈
  round1-radius-km: 2
  round1-top-n: 5
  round1-delay-seconds: 30
  # 第二轮：扩大到 4km 内 Top15，60 秒无人接单后进入第三轮
  round2-radius-km: 4
  round2-top-n: 15
  round2-delay-seconds: 60
  # 第三轮：扩大到 6km 内更多骑手，不再继续自动扩圈
  round3-radius-km: 6
  round3-top-n: 50

chat:
  websocket:
    port: 4400
    path: /ws
    boss: 2
    work: 20


# 开发环境配置
server:
  # 服务器的HTTP端口，默认为8080
  port: 8081
  servlet:
    # 应用的访问路径
    context-path: /
  # undertow 配置
  undertow:
    # HTTP post内容的最大大小。当值为-1时，默认值为大小是无限的
    max-http-post-size: -1
    # 以下的配置会影响buffer,这些buffer会用于服务器连接的IO操作,有点类似netty的池化内存管理
    # 每块buffer的空间大小,越小的空间被利用越充分
    buffer-size: 512
    # 是否分配的直接内存
    direct-buffers: true
    threads:
      # 设置IO线程数, 它主要执行非阻塞的任务,它们会负责多个连接, 默认设置每个CPU核心一个线程
      io: 8
      # 阻塞任务线程池, 当执行类似servlet请求阻塞操作, undertow会从这个线程池中取得线程,它的值设置取决于系统的负载
      worker: 256

# Spring配置
spring:
  # 资源信息
  messages:
    # 国际化资源文件路径
    basename: messages
  # 文件上传
  servlet:
    multipart:
      # 单个文件大小
      max-file-size: 10MB
      # 设置总上传的文件大小
      max-request-size: 20MB
  mvc:
    format:
      date-time: yyyy-MM-dd HH:mm:ss
  jackson:
    # 日期格式化
    date-format: yyyy-MM-dd HH:mm:ss
    serialization:
      # 格式化输出
      indent_output: false
      # 忽略无法转换的对象
      fail_on_empty_beans: false
      write-dates-as-timestamps: false
    deserialization:
      # 允许对象忽略json中不存在的属性
      fail_on_unknown_properties: false
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: fuu
    password: fuu_run_123456
    virtual-host: /
    listener:
      simple:
        missing-queues-fatal: false
        retry:
          enabled: true
          initial-interval: 1000ms
          max-attempts: 3
          multiplier: 2


--- # 数据源配置
spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    # jdbc 所有参数配置参考 https://lionli.blog.csdn.net/article/details/122018562
    # rewriteBatchedStatements=true 批处理优化 大幅提升批量插入更新删除性能(对数据库有性能损耗 使用批量操作应考虑性能问题)
    url: jdbc:mysql://localhost:3308/fuudb?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8&autoReconnect=true&rewriteBatchedStatements=true
    username: root
    password: fuu_run_123456

# MyBatisPlus配置
# https://baomidou.com/config/
mybatis-plus:
  # 不支持多包, 如有需要可在注解配置 或 提升扫包等级
  # 例如 com.**.**.mapper
  mapperPackage: com.karrecy.**.mapper
  # 对应的 XML 文件位置
  mapperLocations: classpath*:mapper/**/*Mapper.xml
  # 实体扫描，多个package用逗号或者分号分隔
  typeAliasesPackage: com.karrecy.**.domain
  # 启动时是否检查 MyBatis XML 文件的存在，默认不检查
  checkConfigLocation: false
  configuration:
    # 自动驼峰命名规则（camel case）映射
    mapUnderscoreToCamelCase: true
    # MyBatis 自动映射策略
    # NONE：不启用 PARTIAL：只对非嵌套 resultMap 自动映射 FULL：对所有 resultMap 自动映射
    autoMappingBehavior: PARTIAL
    # MyBatis 自动映射时未知列或未知属性处理策
    # NONE：不做处理 WARNING：打印相关警告 FAILING：抛出异常和详细信息
    autoMappingUnknownColumnBehavior: NONE
    # 更详细的日志输出 会有性能损耗 org.apache.ibatis.logging.stdout.StdOutImpl
    # 关闭日志记录 (可单纯使用 p6spy 分析) org.apache.ibatis.logging.nologging.NoLoggingImpl
    # 默认日志输出 org.apache.ibatis.logging.slf4j.Slf4jImpl  org.apache.ibatis.logging.nologging.NoLoggingImpl
    logImpl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    # 是否打印 Logo banner
    banner: true
    dbConfig:
      # 主键类型
      # AUTO 自增 NONE 空 INPUT 用户输入 ASSIGN_ID 雪花 ASSIGN_UUID 唯一 UUID
      idType: ASSIGN_ID
      # 逻辑已删除值
      logicDeleteValue: 2
      # 逻辑未删除值
      logicNotDeleteValue: 0
      # 字段验证策略之 insert,在 insert 的时候的字段验证策略
      # IGNORED 忽略 NOT_NULL 非NULL NOT_EMPTY 非空 DEFAULT 默认 NEVER 不加入 SQL
      insertStrategy: NOT_NULL
      # 字段验证策略之 update,在 update 的时候的字段验证策略
      updateStrategy: NOT_NULL
      # 字段验证策略之 select,在 select 的时候的字段验证策略既 wrapper 根据内部 entity 生成的 where 条件
      where-strategy: NOT_NULL

springdoc:
  api-docs:
    path: /v3/api-docs
    enabled: true

# security配置
security:
  # 排除路径
  excludes:
    # 静态资源
    - /*.html
    - /**/*.html
    - /**/*.css
    - /**/*.js
    # 公共路径
    - /favicon.ico
    - /error
    # swagger 文档配置
    - /*/api-docs
    - /*/api-docs/**
    # actuator 监控配置
    - /actuator
    - /actuator/**

--- # redis 单机配置(单机与集群只能开启一个另一个需要注释掉)
spring:
  redis:
    # 地址
    host: localhost
    # 端口，默认为6379
    port: 6381
    # 数据库索引
    database: 9
    # 密码(如没有密码请注释掉)
    # password:
    # 连接超时时间
    timeout: 10s
    # 是否开启ssl
    ssl: false

redisson:
  # redis key前缀
  keyPrefix:
  # 线程池数量
  threads: 4
  # Netty线程池数量
  nettyThreads: 8
  # 单节点配置
  singleServerConfig:
    # 最小空闲连接数
    connectionMinimumIdleSize: 8
    # 连接池大小
    connectionPoolSize: 32
    # 连接空闲超时，单位：毫秒
    idleConnectionTimeout: 10000
    # 命令等待超时，单位：毫秒
    timeout: 3000
    # 发布和订阅连接池大小
    subscriptionConnectionPoolSize: 50

# 防止XSS攻击
xss:
  # 过滤开关
  enabled: true
  # 排除链接（多个用逗号分隔）
  excludes:
  # 匹配链接
  urlPatterns: /**

# Sa-Token配置
sa-token:
  # token名称 (同时也是cookie名称)
  token-name: Authorization
  # token有效期 设为一天 (必定过期) 单位: 秒
  timeout: 86400
  # 多端不同 token 有效期 可查看 LoginHelper.loginByDevice 方法自定义
  # token最低活跃时间 (指定时间无操作就过期) 单位: 秒
  active-timeout: 1800
  # 允许动态设置 token 有效期
  dynamic-active-timeout: true
  # 是否允许同一账号并发登录 (为true时允许一起登录, 为false时新登录挤掉旧登录)
  is-concurrent: false
  # 在多人登录同一账号时，是否共用一个token (为true时所有登录共用一个token, 为false时每次登录新建一个token)
  is-share: true
  # 是否尝试从header里读取token
  is-read-header: true
  # 是否尝试从cookie里读取token
  is-read-cookie: false
  # token前缀
  token-prefix: "Bearer"
  # jwt秘钥
  jwt-secret-key: fuu-run-jwt-T9U3lI1oap6jRfk4w7Ht0Bc5JSyi2XLvO8bCKYhqdzgsEGuN
  auto-renew: true

wx:
  miniapp:
    configs:
      - appid: wx5f2d25061e510def
        secret: 78c176773bd48976418758ea10897328
        token: #你的token
        aesKey: #你的aesKey
        msgDataFormat: #你的msgDataFormat

  pay: #微信服务商支付
      enabled: false
      appId: wx5f2d25061e510def
      mchId: #你的mchId
      returnUrl: http://你的域名/notify/payNotify
      refundUrl: http://你的域名/notify/refundNotify
#      returnUrl: https://你的域名/dev/notify/payNotify
#      refundUrl: https://你的域名/dev/notify/refundNotify
      apiV3Key: #你的apiV3Key
      privateKeyPath: classpath:cert/apiclient_key.pem
      privateCertPath: classpath:cert/apiclient_cert.pem



